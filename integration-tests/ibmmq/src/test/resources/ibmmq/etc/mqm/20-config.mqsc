SET CHLAUTH('DEV.ADMIN.SVRCONN') TYPE(USERMAP) CLNTUSER('app') USERSRC(MAP) MCAUSER('app') ACTION(REPLACE)

DEFINE QLOCAL('ARTEMIS.TO.IBMMQ') REPLACE
SET AUTHREC PROFILE('ARTEMIS.TO.IBMMQ') OBJTYPE(QUEUE) PRINCIPAL('app') AUTHADD(BROWSE, GET, PUT, INQ)

DEFINE QLOCAL('IBMMQ.TO.ARTEMIS') REPLACE
SET AUTHREC PROFILE('IBMMQ.TO.ARTEMIS') OBJTYPE(QUEUE) PRINCIPAL('app') AUTHADD(BROWSE, GET, PUT, INQ)
